package com.alg.datastructure;import junit.framework.TestCase;import java.util.HashSet;import java.util.LinkedList;import java.util.List;import java.util.Set;/** * @author Yasin Zhang */public class SkipListV2Test extends TestCase {    public void test() {        SkipListV2 skipList = new SkipListV2();        //测试跳跃表性能        for (int i = 0; i < 100; i++) {            skipList.insert(i);        }        System.out.println("find(55):" + skipList.find(55));        skipList.delete(55);        System.out.println("find(55):" + skipList.find(55));    }}